# Development Environment Variables
VITE_API_URL=http://localhost:3001/api
VITE_USE_API=true
PORT=3001

# Production Environment Variables
# For OPSI A (FE Hostinger + BE VPS):
# VITE_API_URL=https://your-vps-domain.com/api
# VITE_USE_API=true
# ALLOWED_ORIGINS=https://your-hostinger-domain.com

# For Netlify (FE Netlify + BE VPS):
# VITE_API_URL=https://your-vps-domain.com/api
# VITE_USE_API=true
# ALLOWED_ORIGINS=https://your-netlify-domain.netlify.app,https://your-custom-domain.com

# Static Mode (No API - Use Default Data Only):
# VITE_USE_API=false
# Note: When VITE_USE_API=false, website will use static/default data without fetching from API
# This is useful when backend is unavailable or for static hosting without backend

# Backend Server (VPS)
# PORT=3001
# ALLOWED_ORIGINS=https://your-hostinger-domain.com,https://your-netlify-domain.netlify.app

